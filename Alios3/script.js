document.addEventListener('mousemove', function(event) {  const ponto = document.createElement('div');  ponto.style.position = 'fixed';  ponto.style.width = '8px';  ponto.style.height = '8px';  ponto.style.borderRadius = '50%';  ponto.style.background = 'red';  ponto.style.left = event.clientX + 'px';  ponto.style.top = event.clientY + 'px';  ponto.style.pointerEvents = 'none';  ponto.style.opacity = '0.8';  ponto.style.transition = 'opacity 0.5s linear';  document.body.appendChild(ponto);  setTimeout(() => {    ponto.style.opacity = '0';    setTimeout(() => ponto.remove(), 500);  }, 50);});
